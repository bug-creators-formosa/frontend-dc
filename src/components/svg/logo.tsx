import { LucideMegaphone } from "lucide-react";

export function Logo(props: { className?: string }) {
  return <LucideMegaphone className={props.className} />;
}

export function WhiteLogo(props: { className?: string }) {
  return <LucideMegaphone className={props.className} />;
}
